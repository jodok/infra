DATABASE_URL=postgres://paperclip:{{ salt['pillar.get']('secrets:vault:paperclip:db_password') | trim }}@localhost:5432/paperclip
PAPERCLIP_DEPLOYMENT_MODE=authenticated
PAPERCLIP_DEPLOYMENT_EXPOSURE=public
PAPERCLIP_PUBLIC_URL=https://paperclip.namche.ai
BETTER_AUTH_SECRET={{ salt['pillar.get']('secrets:vault:paperclip:better_auth_secret') | trim }}
PORT=3100
HOST=0.0.0.0
SERVE_UI=true
NODE_ENV=production
PAPERCLIP_TELEMETRY_DISABLED=1
DO_NOT_TRACK=1
